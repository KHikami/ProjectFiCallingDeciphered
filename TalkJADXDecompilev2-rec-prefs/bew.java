package p000;

import java.util.concurrent.TimeUnit;

public final class bew {
    public static final long f3165a = TimeUnit.SECONDS.toMillis(10);
    static final long f3166b = TimeUnit.DAYS.toMillis(1);
}
