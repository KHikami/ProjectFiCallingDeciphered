package p000;

import android.app.Activity;
import android.content.Intent;
import com.google.android.gms.common.api.Status;

public final class haa implements hal {
    static final Status f16462a = new Status(13);

    public gum<Status> mo2436a(gui gui, Activity activity, Intent intent) {
        return gui.mo2510a(new hab(this, gui, intent, gwb.al() ? gwb.m2165e(activity) : null, activity));
    }
}
