package p000;

import java.nio.ByteBuffer;

public final class asz implements auj<byte[], ByteBuffer> {
    public auh<byte[], ByteBuffer> mo380a(aup aup) {
        return new asy(new ata(this));
    }
}
