package p000;

import java.io.InputStream;
import java.net.URL;

public final class avs implements auj<URL, InputStream> {
    public auh<URL, InputStream> mo380a(aup aup) {
        return new avr(aup.m4063a(atu.class, InputStream.class));
    }
}
