package p000;

import android.net.Uri;
import java.io.InputStream;

public final class avi implements auj<Uri, InputStream> {
    public auh<Uri, InputStream> mo380a(aup aup) {
        return new avh(aup.m4063a(atu.class, InputStream.class));
    }
}
