final class awk extends awf {
    awk() {
    }

    public float a(int i, int i2, int i3, int i4) {
        return Math.min(((float) i3) / ((float) i), ((float) i4) / ((float) i2));
    }

    public int a() {
        return awm.b;
    }
}
