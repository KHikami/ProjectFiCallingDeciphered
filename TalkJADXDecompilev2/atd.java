package defpackage;

import java.io.InputStream;

public final class atd implements auj<byte[], InputStream> {
    public auh<byte[], InputStream> a(aup aup) {
        return new asy(new ate(this));
    }
}
