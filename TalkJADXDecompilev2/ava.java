package defpackage;

public final class ava<Model> implements auh<Model, Model> {
    public aui<Model> a(Model model, int i, int i2, anz anz) {
        return new aui(new bag(model), new avc(model));
    }

    public boolean a(Model model) {
        return true;
    }
}
